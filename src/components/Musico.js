import React from 'react'

const Musico = ({registro, handlerClick}) => {
    
    const { email, contraseña, nombre, apellido, ubicacion, fechaDeNacimiento, generoMusicalPreferido, queInstrumentoTocas, estasBuscandoBanda, infomracionAdicional } = registro; 

    return (
        <div className="registro">
        </div>
    )
}

export default Musico